# ---------------------------------------------------------------------

def area(width, length):
    
    return width * length

def perimeter(width, length):
    
    return (width * 2) + (length * 2)